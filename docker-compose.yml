services:
  tpdb-provider:
    build: .
    image: tpdb-provider:latest
    container_name: tpdb-provider
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      - TZ=America/New_York
    env_file:
      - .env
