services:
  tpdb-provider:
    build: .
    image: ghcr.io/o51r15/tpdb-provider:dev
    container_name: tpdb-provider
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      - TZ=America/New_York
    env_file:
      - .env
